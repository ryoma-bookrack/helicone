{
  "name": "helicone-monorepo",
  "private": true,
  "workspaces": [
    "web",
    "packages/*",
    "valhalla/jawn",
    "e2e"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:web": "yarn workspace helicone build",
    "prepare": "husky",
    "lint": "yarn workspace helicone run lint",
    "lint:fix": "yarn workspace helicone run lint-fix"
  },
  "version": "0.1.0",
  "resolutions": {
    "d3-color": "^3.1.0",
    "ws": "^8.17.1",
    "@peculiar/x509": "1.13.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "ts-jest": "^29.4.1"
  }
}
